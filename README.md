# TNC-Automator
This PowerShell tool automates network validation using the tnc (Test-NetConnection) cmdlet. It sequentially tests DNS, ICMP, and TCP port status. It features conditional escalation, running a TraceRoute only if port tests fail, and captures all results in a timestamped forensic log for enterprise troubleshooting
